// we use the 'var' keyword to declare
// declaring a variable named 'person'
var person;

// assigning (initializing) the variable person
person = 'john'

// printing the value stored inside the 'person variable
console.log(person)