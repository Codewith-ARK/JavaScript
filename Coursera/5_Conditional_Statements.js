/*
    There are (2) types of conditional statments
    > if-else
    > swtich
*/

// the most basic conditional statement
if(1 == 2) {
    // if the condition specified in the paranthese return 'true'
    console.log("1 is equal to 2") // code executed
}

// If the first condition does works. Check this one.
else if(1 < 2){
    console.log("1 is less than 2")
}
// if none of the condition return 'true'. Execute this block.
else {
    console.log("1 is not equal to 2")
}
